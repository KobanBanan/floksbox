# ✅ Чеклист подготовки к деплою

## 📦 Перед коммитом в Git

### 1. Проверка .gitignore
- [ ] `backend/venv/` исключен
- [ ] `backend/db.sqlite3` исключен
- [ ] `backend/__pycache__/` исключен
- [ ] `frontend/node_modules/` исключен
- [ ] `.env` файлы исключены
- [ ] `*.log` файлы исключены
- [ ] `backend/media/products/*` исключен (но папка сохранена)

### 2. Проверка файлов
- [ ] `env.example` создан и заполнен
- [ ] `DEPLOY.md` содержит инструкции
- [ ] `README.md` обновлен
- [ ] `docker-compose.yml` настроен правильно
- [ ] `requirements.txt` актуален
- [ ] `frontend/package.json` актуален

### 3. Проверка конфигурации
- [ ] `backend/floksbox_backend/settings.py` - DEBUG=False для production
- [ ] ALLOWED_HOSTS настроен
- [ ] CSRF_TRUSTED_ORIGINS настроен
- [ ] Секретные ключи НЕ закоммичены

### 4. Тестирование
- [ ] Локально все работает с Docker
- [ ] API отвечает корректно
- [ ] Frontend загружается
- [ ] Статические файлы отдаются
- [ ] Медиа файлы отдаются

## 🚀 Перед деплоем на VPS

### 1. Подготовка сервера
- [ ] Docker установлен
- [ ] Docker Compose установлен
- [ ] Git установлен
- [ ] Порты открыты (80, 443, 3000, 8000)
- [ ] Firewall настроен

### 2. Настройка окружения
- [ ] Репозиторий склонирован
- [ ] `.env` файл создан из `env.example`
- [ ] Все переменные в `.env` заполнены
- [ ] Секретный ключ Django сгенерирован
- [ ] DEBUG=False
- [ ] ALLOWED_HOSTS содержит домен/IP

### 3. Запуск
- [ ] `docker-compose build` выполнен успешно
- [ ] `docker-compose up -d` выполнен успешно
- [ ] Контейнеры запущены (`docker-compose ps`)
- [ ] Логи проверены (`docker-compose logs`)

### 4. Проверка работы
- [ ] Сайт открывается по домену/IP
- [ ] API отвечает (`/api/`)
- [ ] Статические файлы загружаются
- [ ] Медиа файлы загружаются
- [ ] Админка Django доступна

### 5. Безопасность
- [ ] HTTPS настроен (Let's Encrypt)
- [ ] Nginx настроен как reverse proxy
- [ ] Админка защищена
- [ ] Резервное копирование настроено

## 📝 Команды для проверки

```bash
# Проверка что не закоммичено лишнее
git status

# Проверка что .env не в Git
git check-ignore .env

# Проверка Docker конфигурации
docker-compose config

# Проверка что контейнеры запущены
docker-compose ps

# Просмотр логов
docker-compose logs -f
```

